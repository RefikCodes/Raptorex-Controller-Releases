<UserControl x:Class="CncControlApp.Controls.PanelJogCanvasView"
     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
             d:DesignHeight="250" d:DesignWidth="640">

    <!-- Compact workspace visualization canvas -->
    <Border Background="#FF2D2D30"
   BorderBrush="#FFA2A2A4"
            BorderThickness="1"
      CornerRadius="10"
            Padding="8">
        <Grid x:Name="Root" Background="#FF2D2D30"
              SizeChanged="Root_SizeChanged">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="303*"/>
                <ColumnDefinition Width="8*"/>
            </Grid.ColumnDefinitions>
            <Canvas x:Name="GridLinesCanvas" IsHitTestVisible="False" Grid.ColumnSpan="2"/>
            <Canvas x:Name="CrosshairCanvas" IsHitTestVisible="False" Grid.ColumnSpan="2"/>
            <Canvas x:Name="InteractionCanvas"
        MouseLeftButtonDown="InteractionCanvas_MouseLeftButtonDown"
       TouchDown="InteractionCanvas_TouchDown" Grid.ColumnSpan="2" Opacity="0.2"/>
        </Grid>
    </Border>
</UserControl>
