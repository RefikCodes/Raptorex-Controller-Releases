<Window x:Class="CncControlApp.Controls.StreamingPopup"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Title="Streaming"
 WindowStyle="None"
 ResizeMode="NoResize"
 SizeToContent="WidthAndHeight"
 Background="#FF1E1E20"
 AllowsTransparency="True"
 WindowStartupLocation="CenterOwner"
 ShowInTaskbar="False" Topmost="True">
 <Border CornerRadius="14" Background="#FF2C2C2F" BorderBrush="#FF5A5A5C" BorderThickness="1.2" Padding="0" SnapsToDevicePixels="True">
 <Border.Effect>
 <DropShadowEffect Color="#000000" BlurRadius="16" ShadowDepth="0" Opacity="0.6"/>
 </Border.Effect>
 <Grid Margin="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions> <!-- Header -->
 <Border Grid.Row="0" Background="#FF3D3D40" Padding="18,14" CornerRadius="14,14,0,0">
 <DockPanel>
 <Ellipse Width="10" Height="10" Fill="#FF34C759" VerticalAlignment="Center"/>
 <TextBlock x:Name="TitleText"
 Margin="10,0,0,0"
 Text="Streaming"
 Foreground="White"
 FontSize="22"
 FontWeight="SemiBold"/>
 </DockPanel>
 </Border>

 <!-- Optional message/description -->
 <Border Grid.Row="1" Background="#FF2C2C2F" Padding="22,10,22,0">
 <TextBlock x:Name="SubtitleText"
 Text=""
 TextWrapping="Wrap"
 Width="200"
 Foreground="#FFDDDDDD"
 FontSize="15"/>
 </Border>

 <!-- Live tolerance line -->
 <Border Grid.Row="2" Background="#FF2C2C2F" Padding="22,6,22,0">
 <TextBlock x:Name="LiveLine"
 Text=""
 TextWrapping="Wrap"
 Width="200"
 Foreground="#FFEEEEEE"
 FontFamily="Consolas"
 FontSize="14"/>
 </Border>

 <!-- Spacer -->
 <Border Grid.Row="3" Background="Transparent"/>

 <!-- Streaming log -->
 <Border Grid.Row="4" Background="#FF3A3A3E" Padding="14" CornerRadius="12" Margin="22,0,22,0">
 <ScrollViewer x:Name="LogScroll" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Height="176" Width="220">
 <TextBlock x:Name="LogText" FontFamily="Consolas" FontSize="13" Foreground="#FFCCCCCC" TextWrapping="Wrap"/>
 </ScrollViewer>
 </Border>

 <!-- Cancel button -->
 <Border x:Name="CancelButtonBorder" Grid.Row="5" Background="Transparent" Padding="22,12,22,14">
 <Button x:Name="CancelButton" Content="İptal" Width="100" Height="32" 
 Background="#C42B1C" Foreground="White" FontWeight="SemiBold"
 HorizontalAlignment="Center" Click="CancelButton_Click"/>
 </Border>
 </Grid>
 </Border>
</Window>