<Window x:Class="CncControlApp.Controls.ResumeConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kaldığı Yerden Devam Et"
        Width="550" Height="620"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FF1E1E20"
        WindowStyle="None"
        AllowsTransparency="True"
        ShowInTaskbar="False" Topmost="True">
    
    <Border CornerRadius="14" Background="#FF2C2C2F" BorderBrush="#FF5A5A5C" BorderThickness="1.2" Padding="0" SnapsToDevicePixels="True">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="16" ShadowDepth="0" Opacity="0.6"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Border Grid.Row="0" Background="#FF3D3D40" Padding="16,12" CornerRadius="14,14,0,0">
                <DockPanel>
                    <Ellipse Width="10" Height="10" Fill="#FF34C759" VerticalAlignment="Center"/>
                    <TextBlock Text="▶️ Kaldığı Yerden Devam Et" 
                               Margin="10,0,0,0"
                               Foreground="White" FontSize="18" FontWeight="SemiBold"/>
                    <Button Content="✕" Width="28" Height="28" 
                            HorizontalAlignment="Right" DockPanel.Dock="Right"
                            Background="Transparent" Foreground="#FFAAAAAA" BorderThickness="0"
                            FontSize="14" Cursor="Hand"
                            Click="CloseButton_Click"/>
                </DockPanel>
            </Border>
            
            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="16,12,16,12">
                <!-- Stop Info - Kompakt -->
                <Border Background="#FF3D3D40" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="📍 Satır:" Foreground="#aaa" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" x:Name="txtStoppedLine" Foreground="White" FontWeight="Bold" Margin="6,0,20,0" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="Toplam:" Foreground="#aaa" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="3" x:Name="txtTotalLines" Foreground="White" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    </Grid>
                </Border>
                
                <!-- ℹ️ Otomatik Pozisyonlama Bilgisi -->
                <Border Background="#FF203040" BorderBrush="#FF00BFFF" BorderThickness="1.5" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="ℹ️ Otomatik Pozisyonlama" 
                                   Foreground="#FF00BFFF" FontWeight="Bold" FontSize="13"/>
                        <TextBlock Text="Z önce 30mm güvenli yüksekliğe çekilecek, sonra XY konumuna gidilecek, ardından Z çalışma yüksekliğine indirilecek." 
                                   Foreground="#FFB0D0E0" FontSize="11" TextWrapping="Wrap" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
                
                <!-- Hedef Koordinatlar -->
                <Border Background="#FF3D3D40" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="🎯 Hedef Koordinatlar (Otomatik Gidilecek)" Foreground="#FFDDDDDD" FontWeight="Bold" FontSize="12"/>
                        <Grid Margin="0,6,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <TextBlock Text="X:" Foreground="#FF00BFFF" FontWeight="Bold" FontSize="14"/>
                                <TextBlock x:Name="txtLastX" Foreground="White" FontWeight="Bold" FontSize="14" Margin="4,0,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <TextBlock Text="Y:" Foreground="#FF00BFFF" FontWeight="Bold" FontSize="14"/>
                                <TextBlock x:Name="txtLastY" Foreground="White" FontWeight="Bold" FontSize="14" Margin="4,0,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2" Orientation="Horizontal">
                                <TextBlock Text="Z:" Foreground="#FF00BFFF" FontWeight="Bold" FontSize="14"/>
                                <TextBlock x:Name="txtLastZ" Foreground="White" FontWeight="Bold" FontSize="14" Margin="4,0,0,0"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
                
                <!-- ⚠️ Koordinat Sıfır Kontrolü -->
                <Border Background="#FF4A3A20" BorderBrush="#FFFFC107" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="⚠️ Koordinat Kayması Kontrolü" Foreground="#FFFFC107" FontWeight="Bold" FontSize="12"/>
                        <TextBlock Text="Work Zero (G-Code sıfır noktası) değişmediyse, makine doğru koordinata gidecektir." 
                                   Foreground="#FFE0D0A0" FontSize="11" Margin="0,4,0,6"/>
                        <CheckBox x:Name="chkZeroConfirmed" Foreground="White" FontSize="12">
                            <CheckBox.Content>
                                <TextBlock Text="Koordinat sıfırlarının doğru olduğunu onaylıyorum" Foreground="White"/>
                            </CheckBox.Content>
                        </CheckBox>
                    </StackPanel>
                </Border>
                
                <!-- Modal State -->
                <Border Background="#FF3D3D40" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="⚙️ Modal State" Foreground="#FFDDDDDD" FontWeight="Bold" FontSize="12"/>
                        <TextBlock x:Name="txtModalSummary" Foreground="#FF00FF88" FontFamily="Consolas" FontSize="10" TextWrapping="Wrap" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
                
                <!-- Preamble -->
                <Border Background="#FF3D3D40" CornerRadius="6" Padding="10">
                    <StackPanel>
                        <TextBlock Text="📋 Preamble" Foreground="#FFDDDDDD" FontWeight="Bold" FontSize="12"/>
                        <TextBlock x:Name="txtPreambleInfo" Foreground="#aaa" FontSize="11" Margin="0,4,0,4"/>
                        <CheckBox x:Name="chkRunPreamble" Content="Preamble'ı çalıştır" 
                                  Foreground="White" FontSize="11" IsChecked="True"/>
                    </StackPanel>
                </Border>
            </StackPanel>
            
            <!-- Buttons -->
            <Border Grid.Row="2" Background="#FF3D3D40" Padding="16,12" CornerRadius="0,0,14,14">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="btnConfirm" Content="✅ Devam Et" Width="140" Height="38" 
                            Background="#FF34C759" Foreground="White" FontWeight="Bold" FontSize="13"
                            BorderThickness="0" Margin="0,0,12,0"
                            Cursor="Hand" Click="ConfirmButton_Click" IsEnabled="False">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="8,4">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button Content="❌ İptal" Width="140" Height="38"
                            Background="#FFFF3B30" Foreground="White" FontWeight="Bold" FontSize="13"
                            BorderThickness="0" Cursor="Hand"
                            Click="CancelButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="8,4">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
